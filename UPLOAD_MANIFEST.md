# 上传清单

本清单解释从本地工作区 `F:\计图` 上传到 GitHub 的资料范围。目标是保留“从数据到最终提交”的完整复现链路，同时避免公开密钥、GitHub 单文件超限以及大量可重建的重复产物。

## 已纳入版本控制

- 热身赛全部核心代码、脚本、Cora 数据、starter 包与最终小型结果包。
- 正式赛全部关键算法版本源码（v29～v62）、基线、运行脚本、融合与门控脚本。
- 正式赛原始数据压缩包 `formal_track1/downloads/track1_data.zip`。
- 最终独立方案结果 `formal_track1/result_v69_d1v65_1200_d2v62_local.zip`。
- 本地实验的日志、JSON 元数据与融合报告，用于还原版本演进和指标。
- 算法文档及其生成脚本。

## 明确排除

- `.secrets/`、`.env*`、私钥和任何本地认证材料。
- `.cache/`、`__pycache__/`、Kaggle 临时 kernel 目录和文档 QA 临时文件。
- 解压后的 CSV 数据。正式赛 CSV 已包含在唯一的原始压缩包中；热身赛数据已保留为 `data/cora.pkl`。
- 100 多份中间提交 ZIP 及对应输出 CSV。它们是不同版本运行后可重建的产物，总计约 17GB，并包含 39 个超过 GitHub 100MB 单文件上限的文件。
- 外部/open 提交原件：当前工作区未单独保留，因此 V71～V94 中依赖它们的融合不能从本仓库完全重建。

## 最终自包含复现边界

可完全重建的正式赛主体是 V69：dataset1 使用 v65 profile 与 1200 棵树，dataset2 使用 v61 profile 与 850 棵树。仓库保留了该方案源码、数据、配置元数据、结果包和算法解释。

